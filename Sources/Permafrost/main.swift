// Placeholder entry point — replaced by the AppKit shell in M2 (see docs/PROJECT_PLAN.md).
print("Permafrost \(PermafrostVersion.string) — core scaffold; app shell arrives in M2")

enum PermafrostVersion {
    static let string = "0.1.0-dev"
}
